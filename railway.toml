[build]
builder = "DOCKERFILE"

[deploy]
startCommand = "sh -c 'gunicorn -k uvicorn.workers.UvicornWorker -w 1 -b 0.0.0.0:${PORT:-8000} api.main:app'"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
